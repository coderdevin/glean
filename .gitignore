# OS junk
.DS_Store
Thumbs.db

# Logs
*.log

# Local env files — committed only as `.dev.vars.example` / `.env.example`.
.dev.vars
.env
.env.local
.env.*.local

# Node / build outputs (mirror app/.gitignore in case of stray copies at root)
node_modules/
dist/
.astro/
.wrangler/
.gstack/

# Misc scratch
uploads/
.thumbnail
.local/
